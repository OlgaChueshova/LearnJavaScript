const idTable = document.getElementById('age-table');
console.log(idTable);

const lableCollection = idTable.getElementsByTagName('label');
console.log(lableCollection);


const form = document.getElementsByTagName('form');
const search = form.getElementsByName('search');
console.log(search);

const input = search.get